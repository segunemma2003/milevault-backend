[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.celery"

[deploy]
startCommand = "celery -A app.services.celery_app worker --loglevel=info --concurrency=2 -Q default,media,notifications,kyc,payments"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
